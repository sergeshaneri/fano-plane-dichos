# TODO

Список идей на будущее. Без приоритетов — обсуждать по ходу.

## От Сергея

- **Визуал.** Найти красивое решение для премиум-вида (включая светлую тему).
- **Локализация.** Русская и английская версии (i18n).
- **Тексты.** Более понятные, развёрнутые объяснения соционической математики прямо в интерфейсе.
- **Плоскость Фано для Аспектона.** Сделать аналогичную интерактивную плоскость, но для 8 аспектов. Поддержать два вида:
  - **Вертикальный** (как Модель А Дон Кихота)
  - **Горизонтальный**
- **Признаки Рейнина по разбиениям.** Для каждого разбиения аспектов/функций — показать список признаков Рейнина и их производных, в формулах которых это разбиение участвует.
- **Семантика разбиений.** Добавить смысловое описание каждого из 7 разбиений (что значит «такты Аушры», «дуальный блок» и т.д., как это интерпретируется).
- **Бисимплекс.** Два треугольника с общим основанием — структура, учитывающая дополнительную систему дихотомий Гуленко-Ньюмана.

## Мои предложения

### Интерактивность и навигация
- **URL-state.** Сохранять выбранные узлы / прямую / режим в hash или query: `?nodes=1,5&view=hadamard`. Сделает любое состояние шарабельным (статьи, обсуждения).
- **Hover-tooltip на узле.** При наведении показывать longName + краткое описание полюсов («Экстравертные — направленность на объект...»).
- **Подсветка всех прямых через узел.** Когда выбран **один** узел — подсветить все 3 прямые, проходящие через него (сейчас прямая активируется только при двух узлах).
- **Демо-режим.** Кнопка «Auto-play»: автоматически перебирает все 7 прямых одну за другой с задержкой 2-3 сек.
- **Двойственность.** Переключатель «точки/прямые»: дуальное представление плоскости Фано, где роли точек и прямых меняются местами.

### Связь с ТИМами
- **Привязка к ТИМам.** При выборе двух дихотомий показывать список из 16 ТИМов, разделённых по 4 группам (++, +−, −+, −−). Это и есть «малые группы Рейнина».
- **Сравнение ТИМов.** Выбрать 2-3 ТИМа из списка и наложить их Модели А с дельтой — какие функции совпадают, какие нет.
- **Интеграция с socionics-wiki.** Клики по дихотомиям и линиям ведут к статьям в твоей вики.

### Контент и образование
- **Glossary panel.** Раскрывающийся блок с глоссарием терминов соционики, привязанный к контексту (если выбраны Экстра/Интро — показать только связанные термины).
- **Цитаты-формулы.** Под формулой `D1 × D2 = D3` показывать её академическую запись (например, через MathML или KaTeX).
- **История.** Краткая страница «Откуда плоскость Фано в соционике» — Букалов, Шепетько, Аушра, Гуленко.

### Технические
- **Экспорт.** Кнопка «Сохранить как SVG/PNG» — текущее состояние графа в файл для статей и презентаций.
- **Mobile-first улучшения.** Свайпы по галерее дихотомий, sticky bottom bar с управлением, лучшая раскладка под портретный режим.
- **Тема в localStorage.** Запомнить выбор пользователя между сессиями (после ввода светлой/тёмной).
- **Open Graph image.** Статичная картинка плоскости Фано для превью в Telegram / соцсетях.
- **Favicon.** Минимальный SVG-favicon (Fano-треугольник).
- **SEO.** Серверный пререндер главной страницы или хотя бы корректный meta-description per state через URL.
- **ESLint + Prettier.** Сейчас только tsc-typecheck — добавить linting и форматирование.
- **Тесты.** Минимум — unit-тесты на math-функции (XOR-product, проверка линий FANO_LINES, симметрии Hadamard).

### Структура и архитектура
- **Цветовая система в одном месте.** Если решим вернуться к смене палитр — вынести все цвета в `src/themes/` через CSS variables (а не inline styles, как я пробовал), чтобы Tailwind-классы работали через токены.
- **Контент в data.** Сейчас названия линий и долгие имена дихотомий зашиты в `src/data/socionics.ts`. При появлении i18n — вынести в `locales/ru.json`, `locales/en.json`.
- **Refactor FunctionMatrix.** Логика цвета ячейки разрослась — вынести в чистую функцию с unit-тестами.

---

Когда вернёмся — выбираем 1-2 пункта и катимся.
